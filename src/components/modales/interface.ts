export interface ModalMenu {
  isModalOpen: boolean;
  setIsModalOpen: (isOpen: boolean) => void;
} 

export interface ModalCarritoProps {
  isModalOpen: boolean;
  setIsModalOpen: (isOpen: boolean) => void;
}